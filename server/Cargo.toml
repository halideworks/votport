[package]
name = "votport"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
rust-version = "1.97"
description = "Password-protected file receive portal built on VOT (Verified Object Transfer)"
publish = false

# Standalone build: never join a surrounding cargo workspace.
[workspace]

[dependencies]
vot-sdk = { git = "https://github.com/halideworks/VOT", rev = "d64bd9d6c78a9e264049a2f46eec8b3c5ab6e3a0" }
vot-sdk-file = { git = "https://github.com/halideworks/VOT", rev = "d64bd9d6c78a9e264049a2f46eec8b3c5ab6e3a0" }

axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "signal", "sync"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["fs", "set-header"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
argon2 = "0.5"
rand = "0.8"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
rustix = { version = "1", features = ["process", "fs"] }

[dev-dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "cookies"] }
tempfile = "3"

[profile.release]
lto = "thin"
