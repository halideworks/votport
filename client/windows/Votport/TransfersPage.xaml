<Page x:Class="Votport.TransfersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Votport">
  <Grid RowSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock Text="TRANSFERS" Style="{StaticResource SectionLabel}" />
    <TextBlock x:Name="EmptyText" Grid.Row="1" Text="Nothing yet. Send a drop or receive a delivery."
        Foreground="{StaticResource VotMutedBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" />
    <ListView x:Name="List" Grid.Row="1" SelectionMode="Single" IsItemClickEnabled="True"
        ItemClick="List_ItemClick" SelectionChanged="List_SelectionChanged">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="local:TransferItem">
          <Grid Padding="14" Margin="0,4" Background="{StaticResource VotPanelBrush}" CornerRadius="8" RowSpacing="8">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid ColumnSpacing="10">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <FontIcon Glyph="{x:Bind Icon}" Foreground="{StaticResource VotMutedBrush}" VerticalAlignment="Top" />
              <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock Text="{x:Bind Subject}" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{x:Bind Status, Mode=OneWay}" FontSize="12" Foreground="{StaticResource VotMutedBrush}" TextWrapping="Wrap" />
              </StackPanel>
              <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <Button Content="Cancel" Visibility="{x:Bind Running, Mode=OneWay}" Click="Cancel_Click" />
                <Button Content="Remove" Visibility="{x:Bind NotRunning, Mode=OneWay}" Click="Remove_Click" />
              </StackPanel>
            </Grid>
            <ProgressBar Grid.Row="1" Value="{x:Bind Fraction, Mode=OneWay}" Maximum="100" Foreground="{StaticResource VotProgressBrush}" />
            <ItemsControl Grid.Row="2" ItemsSource="{x:Bind Files, Mode=OneWay}" Visibility="{x:Bind Expanded, Mode=OneWay}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="local:FileRow">
                  <Grid ColumnSpacing="10" Margin="0,2">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="120" />
                      <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{x:Bind Path}" Style="{StaticResource Mono}" FontSize="12" />
                    <ProgressBar Grid.Column="1" Value="{x:Bind Fraction}" Maximum="100" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="2" Text="{x:Bind Label}" FontSize="12" TextAlignment="Right" Foreground="{StaticResource VotMutedBrush}" />
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
